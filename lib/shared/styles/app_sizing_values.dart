import 'package:flutter/cupertino.dart';

class AppValues {

  static const presenterWidgetHeight = 250.0;
  static const defaultVerticalSpace = 20.0;
  static const defaultPadding = 20.0;
  static const smallPadding = 8.0;
  static final defaultBorderRadius = BorderRadius.circular(20);
  static double toggleButtonSize = 60.0;
}